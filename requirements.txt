# —— harvesting & parsing
requests>=2.31.0
python-dotenv>=1.0.0
feedparser>=6.0.11
lxml>=5.1.0
tqdm>=4.66.0
retry>=0.9.2
docling>=2.0.0

# —— RAG stack
openai>=1.30.0          # OpenRouter uses the OpenAI-compatible API
chromadb>=0.5.0         # local vector DB (persistent, no server needed)
tiktoken>=0.7.0         # exact token counting (optional but recommended)
